exports.handler = (event, context, callback) => {
    /*console.log(JSON.parse(event.desired) + event.thingName);
    var AWS = require('aws-sdk');
    AWS.config.region = "ap-southeast-1";
    var iotdata = new AWS.IotData({endpoint: 'a18pjieuamqfus.iot.ap-southeast-1.amazonaws.com'});
    var params = { payload:JSON.stringify({"state": {"desired": JSON.parse(event.desired) } }), thingName:event.thingName };
    try {
        iotdata.updateThingShadow(params, function(err, data) {
            console.log("updateThingShadow");
            if (err) {
                callback(err, err.stack); // an error occurred
            } else {
                callback(null, data);         // successful response
            }
        });
    } catch(t) {
        console.log(JSON.stringify(t));
        callback(null,t);
    }*/
};
